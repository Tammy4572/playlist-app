import React, { Component } from 'react';

export default class NavBar extends Component {
     render(){
          return (
               <nav className=" nav navbar-inverse bg-primary">
                    <a className="navbar-brand">Play What?!</a>
               </nav>
          )
     }
}
